name: One-time Auto Flow v12 follow-up

on:
  pull_request:
    branches: [main]
    types: [synchronize]

permissions:
  contents: write

jobs:
  patch:
    if: github.head_ref == 'ux/perfection-v12'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ux/perfection-v12
          fetch-depth: 0

      - name: Apply follow-up corrections
        run: python3 .github/scripts/v12_followup.py

      - name: Validate final product
        run: |
          node --check a.js
          node --test tests/autoflow.static.test.mjs

      - name: Commit and remove one-time files
        run: |
          rm -f .github/scripts/v12_followup.py
          rm -f .github/workflows/one-time-v12-followup.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "fix: close final v12 interaction gaps"
          git push origin HEAD:ux/perfection-v12
